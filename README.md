# bookmaker-signatures

A simple Vue app to help layout book and zine pages into printable signatures.

## Setup

```bash
npm install
npm run dev
```

## Current status

- Initial landing page with core inputs for signature math
- Folder image upload (expects sequentially named images)
- Auto-calculated signature count with manual override
